import React from 'react';
import { PageShell } from '../../components/PageShell';

export function ClassListingPage() {
  return (
    <PageShell centered>
      <div style={{ padding: 32, textAlign: 'center' }}>
        <p style={{ color: '#888' }}>Classes — Phase 12</p>
      </div>
    </PageShell>
  );
}
