import React from 'react';
import { PageShell } from '../../components/PageShell';

export function CheckinPage() {
  return (
    <PageShell centered>
      <div style={{ padding: 32, textAlign: 'center' }}>
        <p style={{ color: '#888' }}>Check-in — Phase 8</p>
      </div>
    </PageShell>
  );
}
