// @kalpx/feature-flows — headless controller package skeleton
// Controllers live in: mitra/, classes/, community/, auth/
// Each controller must NOT import: react-native, react-router, DOM APIs,
// AsyncStorage, localStorage, expo-*, Stripe SDKs.
// All platform behavior must be injected via adapters from ./types.
export * from './types';